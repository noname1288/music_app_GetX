import 'package:flutter/material.dart';

class ThietBiPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Thiết Bị Page'),
      ),
      body: Center(
        child: Text('Thiết Bị Page Content'),
      ),
    );
  }
}